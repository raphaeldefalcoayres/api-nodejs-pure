export function createFish(request, response) {
  // Implementação da lógica para create fish
  response.json({ message: "Fish create com sucesso" });
}

export function readFish(request, response) {
  // Implementação da lógica para read fish
  response.json({ message: "Fish read com sucesso" });
}

export function showFish(request, response) {
  // Implementação da lógica para show fish
  response.json({ message: "Fish show com sucesso" });
}

export function updateFish(request, response) {
  // Implementação da lógica para update fish
  response.json({ message: "Fish update com sucesso" });
}

export function deleteFish(request, response) {
  // Implementação da lógica para delete fish
  response.json({ message: "Fish delete com sucesso" });
}