export function createDog(request, response) {
  // Implementação da lógica para create dog
  response.json({ message: "Dog create com sucesso" });
}

export function readDog(request, response) {
  // Implementação da lógica para read dog
  response.json({ message: "Dog read com sucesso" });
}

export function showDog(request, response) {
  // Implementação da lógica para show dog
  response.json({ message: "Dog show com sucesso" });
}

export function updateDog(request, response) {
  // Implementação da lógica para update dog
  response.json({ message: "Dog update com sucesso" });
}

export function deleteDog(request, response) {
  // Implementação da lógica para delete dog
  response.json({ message: "Dog delete com sucesso" });
}