export function createProject(request, response) {
  // Implementação da lógica para create project
  response.json({ message: "Project create com sucesso" });
}

export function readProject(request, response) {
  // Implementação da lógica para read project
  response.json({ message: "Project read com sucesso" });
}

export function showProject(request, response) {
  // Implementação da lógica para show project
  response.json({ message: "Project show com sucesso" });
}

export function updateProject(request, response) {
  // Implementação da lógica para update project
  response.json({ message: "Project update com sucesso" });
}

export function deleteProject(request, response) {
  // Implementação da lógica para delete project
  response.json({ message: "Project delete com sucesso" });
}